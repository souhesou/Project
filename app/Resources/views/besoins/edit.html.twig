{% extends 'base.html.twig' %}

{% block container %}
    <div class="container">

    <center>
    <h1>Edit a need</h1>

        <br>
    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <br>
        <input class= "btn btn-round btn-warning" type="submit" value="Edit" />
    {{ form_end(edit_form) }}

       <br>
        {{ form_start(delete_form) }}
        <button class="btn btn-round btn-danger" type="submit" > <i class="fa fa-trash-o"></i>Delete</button>
        {{ form_end(delete_form) }}
<br>
        <a class="btn btn-round btn-info"  href="{{ path('besoins_index') }}"><i class="fa fa-chevron-circle-left"></i> List of needs</a>




    </center>
</div>
{% endblock %}
