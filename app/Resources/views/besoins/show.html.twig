{% extends 'base.html.twig' %}

{% block container %}
    <center>
        <section id="main-content">
            <h1>Details</h1>

            <table class="table table-striped table-advance table-hover">
                <hr>
                <thead>
                <a class="btn btn-round btn-default"  href="{{ path('besoins_index') }}"><i class="fa fa-chevron-circle-left"></i></a>


                <a class="btn btn-round btn-warning" href="{{ path('besoins_edit', { 'id': besoin.id }) }}"><i class="fa fa-pencil"></i></a>

                {{ form_start(delete_form) }}
                <button class="btn btn-round btn-danger" type="submit" > <i class="fa fa-trash-o"></i>   </button>
                {{ form_end(delete_form) }}
                <br>
                <br>
                <br>
                </thead>

        <tbody>
            <tr>
                <th ><span class="badge bg-primary">Number</span></th>
                <td><span class="badge bg-success">{{ besoin.id }}</td>
            </tr>
            <tr>
                <br>
                <th ><span class="badge bg-primary">Category</span></th>
                <br>
                <td><span class="badge bg-success">{{ besoin.nomBs }}</td>
            </tr>
            <tr>
                <th ><span class="badge bg-primary">Quantity</span></th>
                <td><span class="badge bg-success">{{ besoin.quantite }}</td>
            </tr>
        </tbody>
    </table>


        </section></center>
{% endblock %}
