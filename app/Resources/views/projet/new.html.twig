{% extends 'base_front.html.twig' %}

{% block container %}
    <center>
    <h1>Project creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input class="btn-primary" type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a class="btn btn-theme" href="{{ path('p_index') }}">Back to the list</a>
        </li>
    </ul>
    </center>
{% endblock %}
