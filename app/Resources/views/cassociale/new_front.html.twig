{% extends 'base_front.html.twig' %}

{% block container %}
    <h1>Cassociale creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" class="btn-primary" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('sociale_index_front') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
