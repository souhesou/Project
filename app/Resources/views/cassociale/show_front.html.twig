{% extends 'base_front.html.twig' %}

{% block container %}
    <center>
<section id="main-content">

        <h1> <i class="fa fa-angle-right"></i>Show a social case</h1>


                    <table cellpadding="0" cellspacing="0" border="0" class="table table-striped table-advance table-hover" id="hidden-table-info">
                        <hr>
                        <thead>
                        <a class="btn btn-theme" href="{{ path('sociale_index_front') }}"><i class="fa fa-arrow-circle-left"></i> Return to social cases</a>



                        </thead>

        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cassociale.id }}</td>
            </tr>
            <tr>
                <th>Lieu</th>
                <td>{{ cassociale.lieu }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{% if cassociale.date %}{{ cassociale.date|date('F jS ') }}{% endif %}</td>
            </tr>
        </tbody>
    </table>



</section>
    </center>

{% endblock %}
