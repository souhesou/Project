{% extends 'base_front.html.twig' %}

{% block container %}
<link rel="stylesheet" href="{{ asset('front/css/bootstrap.css') }}">
<link rel="stylesheet" href="{{ asset('front/css/animate.css') }}">
<link rel="stylesheet" href="{{ asset('front/css/owl.carousel.min.css') }}">

<link rel="stylesheet" href="{{ asset('front/fonts/ionicons/css/ionicons.min.css') }}">
<link rel="stylesheet" href="{{ asset('front/fonts/fontawesome/css/font-awesome.min.css') }}">
<link rel="stylesheet" href="{{ asset('front/fonts/flaticon/font/flaticon.css') }}">
<link rel="stylesheet" href="{{ asset('front/css/magnific-popup.css') }}">

<!-- Theme Style -->
<link rel="stylesheet" href="{{ asset('front/css/style.css') }}">
    <center>
<section id="main-content">
    <section class="site-hero overlay" data-stellar-background-ratio="0.5" style="background-image: url({{ asset('front/images/nn.jpg') }})";>
        <div class="container">
            <div class="row align-items-center justify-content-center site-hero-inner">
                <div class="col-md-8 text-center">
                    <div class="mb-5 element-animate">
                        <div class="block-17">
                            <h1 class="heading mb-4" >Social issues list</h1>
                            <div class="lead"></div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>



                    <table id="example" cellpadding="0" cellspacing="0" border="0" class="table table-striped table-advance table-hover">
                        <thead>
                        <tr>
                            <th class="hidden-phone">Number</th>
                            <th class="hidden-phone">Adress</th>
                            <th class="hidden-phone">Date</th>

                        </tr>
                        </thead>
                        <tbody>
                        {% for cassociale in cassociales %}


                            <tr class="gradeX" id="search">
                                <td class="center hidden-phone"><i class="fa fa-angle-right"></i><a href="{{ path('sociale_show_front', { 'id': cassociale.id }) }}">{{ cassociale.id }}</a></td>
                                <td class="center hidden-phone">{{ cassociale.lieu }}</td>
                                <td class="center hidden-phone">{% if cassociale.date %}{{ cassociale.date|date('Y-m-d') }}{% endif %}</td>
                                <td class="center hidden-phone">
                                    <ul>

                                            <a class="fa fa-plus-circle" href="{{ path('sociale_show_front', { 'id': cassociale.id }) }}">détails</a>
<br>


                                    </ul>
                                </td>
                            </tr>
                        {% endfor %}
                        <tr>
                            <td></td>
                            <td></td>
                            <td></td>
                            <td><a class="fa fa-edit" href="{{ path('sociale_new_front') }}">Add a social case</a></td>
                        </tr>
                        </tbody>
                    </table>
                    <div class="btn-group">
                        <ul>
                            <div class="navigation">
                                {{ knp_pagination_render(cassociales) }}
                            </div>
                        </ul>
                    </div>


</section>
        <form class="navbar-form">
            <input type="text" id="myInput" onkeyup="search()" placeholder="Search .." title="Type in a name"><br><br>



            </button>

        </form>
        <script>
            function search() {
                $("#myInput").on("keyup", function() {
                    var value = $(this).val().toLowerCase();
                    $("#example #search").filter(function() {
                        $(this).toggle($(this).text().toLowerCase().indexOf(value) > -1)
                    });
                });
            }
        </script>

    </center>
        {% endblock %}
