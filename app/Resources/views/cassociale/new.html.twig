{% extends 'base.html.twig' %}

{% block container %}
    <center>
    <div class="container">
    <h1>Social issue creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input class="btn-primary" type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('sociale_index') }}">Back to the list</a>
        </li>
    </ul>
    </div>
    </center>
{% endblock %}
