{% extends('base_front.html.twig') %}

    {% block container %}
        <section class="site-hero overlay" data-stellar-background-ratio="0.5" style="background-image: url({{ asset('front/images/nn.jpg') }});">
            <div class="container">
                <div class="row align-items-center justify-content-center site-hero-inner">
                    <div class="col-md-8 text-center">

                        <div class="mb-5 element-animate">
                            <div class="block-17">
                                <h1 class="heading mb-4">Helping others is the way we help ourselves.</h1>
                                <p><a href="{{ path('AboutFront') }}" class="btn btn-primary-white py-3 px-5">About Us</a>
                                    <a href="{{ path('camp_esprit')  }}" class="text-white ml-4"> <span class="ion-ios-location mr-2"></span> Visit us </a>
                                </p>
                            </div>
                        </div>

                    </div>
                </div>
            </div>
        <center >
            <div style="#">  <div class="container"   style="width: 300px"; >
                    <div class="container"   style="width: 300px"; >        {% form_theme form 'bootstrap_3_layout.html.twig' %}
                        {# {% form_theme form 'bootstrap_3_horizontal_layout.html.twig' %} #}

                        {{ form(form) }}
                    </div></div>
        </center>

    {% endblock %}